package com.senla.build.config;

public enum SortEnum {

    //Guest
    BY_ALPHABET,
    BY_CHECKOUT_DATE,

    //Room
    BY_ADDITION,
    BY_PRICE,
    BY_CAPACITY,
    BY_STARS,

    //Maintenance
    BY_CATEGORY,
    BY_TIME
}
